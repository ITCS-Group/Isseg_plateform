# URL de base de l'API backend (NestJS), avec le préfixe /api/v1.
# Exposée au client (préfixe NEXT_PUBLIC_) : lue par apps/web/src/lib/api.ts.
# En dev local, le backend écoute par défaut sur le port 3001 (cf. apps/api/.env.example).
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1
